services:
  blog:
    image: ${CI_REGISTRY}/${CI_PROJECT}/${CI_SERVICE}:${CI_TAG}
    restart: unless-stopped
    env_file:
      - .env.frontend.deploy
    ports:
      - "3002:3002"
    networks:
      - app-network-prod

networks:
  app-network-prod:
    external: true